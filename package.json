{
  "name": "cloudflare-mygpt-github",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler d1 migrations apply REPO_DB --remote && wrangler deploy",
    "setup": "wrangler deploy && wrangler d1 migrations apply REPO_DB --remote && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.45.0"
  }
}
