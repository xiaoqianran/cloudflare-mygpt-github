{
  "name": "cloudflare-mygpt-github",
  "version": "0.6.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "setup": "wrangler deploy && wrangler d1 migrations apply REPO_DB --remote"
  },
  "devDependencies": {
    "wrangler": "^4.45.0"
  }
}
